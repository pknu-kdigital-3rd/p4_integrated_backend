fastapi[standard]
httpx
lap>=0.5.12
opencv-python-headless
ultralytics
openai
aiortc
psutil
pydantic-settings
tensorrt-cu13
unidepth @ git+https://github.com/lpiccinelli-eth/UniDepth.git@8d8cfe4c7ee15297099983607febf0d4f32eb3d6

# ultralytics pulls in torch/torchvision transitively, but plain `pip install
# -r requirements.txt` resolves the CPU-only build. For GPU inference, install
# the CUDA build first from PyTorch's index (match the CUDA version to the
# machine's driver, e.g. `nvidia-smi`), then install the rest of this file:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
